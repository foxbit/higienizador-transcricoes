#!/usr/bin/env python3

import os
import sys
import signal
import locale
from pathlib import Path

# Configurar locale
locale.setlocale(locale.LC_ALL, '')

# Sinais
def _handle_sigint(signum, frame):
    print('Encerrando...')
    sys.exit(0)

signal.signal(signal.SIGINT, _handle_sigint)

# Configurar paths
pkgdatadir = os.environ.get('PKGDATADIR', '/usr/share/com.foxbit.higienizador')
pythondir = os.environ.get('PYTHONPATH', sys.prefix)

# Variáveis de ambiente
os.environ.setdefault('GSETTINGS_SCHEMA_DIR', '/usr/share/glib-2.0/schemas')
os.environ.setdefault('XDG_DATA_DIRS', '/usr/share')

# Executar app
from src.main import main

if __name__ == '__main__':
    sys.exit(main())
